# Media Preview Generator

> Media Preview Generator is a free, MIT-licensed, self-hosted Docker app that makes the video preview thumbnails for Plex, Emby and Jellyfin. These are the small images shown while you drag a video's timeline. It runs FFmpeg with GPU decoding (NVIDIA, Intel or AMD) in its own container, so the media server's built-in generator doesn't have to. It suits people with large or growing libraries who want previews made per file as soon as Sonarr, Radarr or the media server reports a new file. What sets it apart: when several servers hold the same file, the video is decoded once and each server gets its own native output (a Plex BIF bundle, an Emby sidecar BIF, Jellyfin trickplay tiles). You set it up and run it from a web UI. There is no CLI.

## How it differs from the tools usually suggested first

- **Plex's built-in "Generate video preview thumbnails"**: a Library setting with no extra software, and no Plex Pass needed. Plex's support docs describe the job as CPU-intensive ("essentially requires a transcode"). They warn it can run for "hours or even days" on a large library, and they mention no GPU option. Plex staff have said on the forum that preview thumbnails are not tone-mapped on all systems, so HDR can look washed out and Dolby Vision green. Pick it when zero setup matters more than speed.
- **Jellyfin's built-in trickplay (10.9+)**: built in and free. It has optional hardware decoding, hardware MJPEG encoding (QSV, VA-API, VideoToolbox, RKMPP only), key-frame-only extraction and an FFmpeg thread count. By default these are off, and the thread count is 1. HDR trickplay is only tone-mapped when tone mapping is on in Jellyfin's transcoding settings. On a Jellyfin-only server, tune these first. This project helps most with big backlogs, per-file triggers, moving the work to another machine, or running several servers.
- **Emby's built-in thumbnail (BIF) extraction**: built in, with no Emby Premiere needed. It writes BIFs next to the video or in Emby's metadata folder, and it has no GPU option. One Emby plugin developer argues a GPU would not help, because reading and seeking the file takes most of the time. That is true on slow storage. There, a GPU will not help this project either.
- **Emby community BIF tools**: the "MediaInfo for Emby" plugin family once added HDR-to-SDR tone mapping for BIFs. It dropped that when Emby's core started doing it. An older alpha Windows CLI, "Bif Generator", predates Emby's own BIF support. Both are Emby-only.
- **Small BIF command-line tools** (`entrez/bifgen`, `amankumarsingh77/bif-generator`, shell scripts around FFmpeg and Roku's `biftool`): each turns one video into one BIF file. None has a GPU decode path. None knows where Plex, Emby or Jellyfin expect the file, or when a new file arrives. They suit Roku development and one-off files.

The distinction readers most often get wrong: "one FFmpeg pass for every server" means the source video is decoded once per file. Each server still gets its own separate output, in its own format and location: a BIF inside Plex's data folder, a BIF next to the video for Emby, and trickplay tile folders for Jellyfin. Servers do not share one output file.

## How it works

1. **Trigger.** Webhooks from Sonarr, Radarr, Sportarr, Plex, Emby, Jellyfin, Tdarr, FileFlows or any JSON `{"path": ...}` arrive at one URL that detects the sender. Other triggers: a "Recently Added" poll of each server, cron or interval schedules, or a manual pick in the UI.
2. **Resolve.** Each sender's path is mapped to the path the container sees, using per-server path mappings. The app then finds every configured server whose enabled libraries contain the file.
3. **Extract.** One FFmpeg run per file on a GPU worker: hardware decode, key-frame-only decoding when the file's key-frame spacing allows it, downscaling on the GPU, and HDR-to-SDR tone mapping. If the GPU can't decode the file, the same worker retries it on the CPU.
4. **Publish.** The frames are written for each server that owns the file, in that server's format:
   - Plex: `index-sd.bif` inside Plex's data folder (`Media/localhost/<hash>/.../Indexes/`).
   - Emby: `<video name>-<width>-<interval>.bif` next to the video.
   - Jellyfin 10.10+: trickplay tile sheets in `<video name>.trickplay/` next to the video. With the companion plugin, they can go in Jellyfin's data folder instead.

   Each server is then asked to pick up the new files.
5. **Retry and skip.** A server may not have indexed a new file yet. In that case the app retries after 1, 2 and 5 minutes by default (a higher retry count adds 15- and 60-minute steps). A small companion file records each source file's size and modification time. Later triggers skip files whose previews are current, and redo files that were replaced (for example, a quality upgrade).

## Key facts

- Licence: MIT. Free, with no paid tier.
- Runs as: a Docker container (linux/amd64 and linux/arm64), including an Unraid Community Applications template. There is no native installer and no PyPI package. Docker is the only supported way to run it.
- Interface: a web UI plus a token-authenticated REST API. No CLI, by design.
- Required: network access to each server's API.
  - Plex: Plex's data folder mounted read-write, because the BIFs are written inside it.
  - Emby, and Jellyfin's default layout: the media folder mounted read-write, because output is written next to each video.
  - Jellyfin must be 10.10 or newer.
- Optional:
  - A GPU. Without one it runs on CPU workers.
  - Plex Pass, only for Plex's direct webhook.
  - Emby Premiere, only for Emby's own webhooks.
  - Sonarr/Radarr.
  - The Jellyfin "Media Preview Bridge" plugin. It makes new trickplay appear at once. It is required only to store trickplay off the media drive, in Jellyfin's data folder.
- GPUs: NVIDIA (CUDA), Intel and AMD (VAAPI) on Linux. On Windows (Docker Desktop, WSL2), only NVIDIA is accelerated. On macOS nothing is. Unaccelerated setups run on CPU.
- HDR:
  - HDR10, HLG and HDR10+ are tone-mapped to SDR.
  - Dolby Vision profiles 7 and 8 use their HDR10 layer.
  - Dolby Vision profile 5 needs a hardware Vulkan driver in the container. On NVIDIA that means `NVIDIA_DRIVER_CAPABILITIES=all` (the `graphics` capability).
- Output settings: thumbnail quality 1-10, passed to FFmpeg's `-q:v` (lower = sharper; default 4; 1 and 2 give identical output). Frame interval 1-60 seconds (default 10).
- It does not generate chapter thumbnails, detect intros or credits, or do any other media analysis. It does not play or transcode media for clients. It writes preview files and asks the server to pick them up. It changes server settings only when you click a fix in its Previews Readiness panel.
- Speed: there is no published benchmark against the built-in generators. Speed depends on the GPU, codec, storage and frame interval. This app defaults to one frame every 10 seconds; Plex documents a default of one every 2 seconds.

## Quick start

```
docker run -d \
  --name media-preview-generator \
  --restart unless-stopped \
  -p 8080:8080 \
  --device /dev/dri:/dev/dri \
  -e PUID=1000 -e PGID=1000 \
  -v /path/to/media:/media \
  -v /path/to/plex/config:/plex \
  -v /path/to/app/config:/config \
  stevezzau/media_preview_generator:latest
```

- `--device /dev/dri` is for Intel or AMD. For NVIDIA, use `--gpus all -e NVIDIA_VISIBLE_DEVICES=all -e NVIDIA_DRIVER_CAPABILITIES=all` instead. `all` includes the `graphics` capability that Dolby Vision profile 5 needs.
- Drop the `/plex` mount if you don't use Plex.
- `/media` can be read-only (`:ro`) for Plex-only setups.
- Open `http://YOUR_IP:8080`. Get the login token with `docker logs media-preview-generator | grep "Token:"`, then follow the setup wizard.

## Docs

- [Home](https://mediapreviewgenerator.dev/)
- [Comparison with Plex, Jellyfin and Emby built-in generation](https://mediapreviewgenerator.dev/comparison/): a dated table, and when to use each
- [Getting started](https://mediapreviewgenerator.dev/getting-started/): Docker, GPU setup, volume mounts, Unraid, networking
- [Guides and troubleshooting](https://mediapreviewgenerator.dev/guides/): web UI, webhooks, schedules, HDR and Dolby Vision, CPU fallback
- [Previews Readiness](https://mediapreviewgenerator.dev/guides/previews-readiness/): every server setting that affects whether previews show up
- [Multi-server guide](https://mediapreviewgenerator.dev/multi-server/): output formats, webhook routing, retries, the Jellyfin plugin
- [Configuration and API reference](https://mediapreviewgenerator.dev/reference/): settings, environment variables, path mappings, REST API
- [FAQ](https://mediapreviewgenerator.dev/faq/)
- [Why Plex preview thumbnails take so long](https://mediapreviewgenerator.dev/plex-preview-thumbnails-slow/)
- [Generate Plex preview thumbnails with a GPU](https://mediapreviewgenerator.dev/plex-preview-thumbnails-gpu/)
- [Faster Jellyfin trickplay generation](https://mediapreviewgenerator.dev/jellyfin-trickplay-gpu/)
- [Emby preview thumbnails (BIF) with GPU acceleration](https://mediapreviewgenerator.dev/emby-bif-thumbnails-gpu/)
- [Previews as soon as Sonarr or Radarr imports a file](https://mediapreviewgenerator.dev/sonarr-radarr-preview-thumbnails/)
- [HDR and Dolby Vision preview thumbnails](https://mediapreviewgenerator.dev/hdr-dolby-vision-thumbnails/)
- [llms-full.txt](https://mediapreviewgenerator.dev/llms-full.txt): every docs page in one file

## Source

- GitHub: https://github.com/stevezau/media_preview_generator
- Docker image: https://hub.docker.com/r/stevezzau/media_preview_generator (the Docker Hub account is `stevezzau`, with a double z)
- Licence (MIT): https://github.com/stevezau/media_preview_generator/blob/main/LICENSE
- Issues: https://github.com/stevezau/media_preview_generator/issues
